---
title: "FFI bindings + safe Rust wrapper (ghostty_vt::Terminal, RenderFrame borrow guard)"
status: open
priority: 1
created-at: "2026-02-23T03:13:04Z"
blockers:
  - shim-002
---

Write raw FFI declarations for all C ABI exports (manual extern C or bindgen). Build safe ghostty_vt::Terminal Rust wrapper enforcing single-thread ownership. Implement RenderFrame<'_> borrow guard: while held, render_update() cannot be called (enforced by &self borrow). Drop impl calls render_clear_dirty(). Expose: new, feed, resize, render_update, row_cells, row_selection, cursor, colors, dirty state, mode flags, key encoding, scroll, selection. Write smoke tests: feed bytes → read rows → verify dirty state → verify cursor position. Refs: docs/architecture/02-data-model.md § Render Data Access and Frame Lifetime Safety, docs/architecture/06-ghostty-shim.md § FFI Lifetime and Thread Contract, docs/architecture/08-ghostty-alignment-decisions.md § Decision 4.

The Key enum integer values must be looked up from `crates/ghostty-vt/zig/ghostty/src/input/key.zig`. The Rust side needs matching constants.
